import { View } from "react-native";
import { Text } from "react-native-paper";

export function DetailScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text variant>Home Screen</Text>
    </View>
  );
}
